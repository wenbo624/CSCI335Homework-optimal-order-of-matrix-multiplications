1. This project will output the optimal number of multiplication and the ordering.

2. There is no error in this assignment.
And I had encountered the segmentation fault: 11.

3. To compile on terminal type:
Using the makefile for Homework 5.

Compile everything by typing

make clean
make all

To delete all .o files and executables By typing

make clean

Compile one program individually by just typing make <Program Name>, for example

make optimal_multiplications

To run:

./optimal_multiplications <file name> 

Example:

./optimal_multiplications dimensions_file.txt

4. The test files are in this directory.